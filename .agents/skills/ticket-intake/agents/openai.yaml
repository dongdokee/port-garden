interface:
  display_name: "Ticket Intake"
  short_description: "Classify request type and collect MRI with approval gates."
  default_prompt: "Use $ticket-intake to infer ticket type, audit MRI completeness, ask one clarifying question per turn, and output an approved handoff ticket."
